import { BoxItemMarginSize } from '@/dashboard/types'
import { TextSize } from '@/utils/ui-kit'

export const marginSizeValues: Record<BoxItemMarginSize, number> = {
  '2xs': 4,
  xs: 8,
  s: 12,
  m: 16,
  l: 20,
  xl: 24,
}
export const fontSizeValues: Record<TextSize, number> = {
  '2xs': 10,
  xs: 12,
  s: 14,
  m: 16,
  l: 18,
  xl: 20,
  '2xl': 24,
  '3xl': 32,
  '4xl': 48,
  '5xl': 72,
  '6xl': 96,
}
